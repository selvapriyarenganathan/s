#include<stdio.h>
#include<conio.h>
int main()
{
	int x,y,z;
	scanf("%d%d%d",&x,&y,&z);
	if((x>y)&&(x>z))
	{
		printf("%d is greater ",a);
	}
	else if((y>x)&&(y>z))
	{
		printf("%d is greater",b);
	}
	else
	{
		printf("%d is greater",c);
	}
}
